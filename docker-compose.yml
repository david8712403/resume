services:
  resume-web:
    container_name: davidchen-resume-web
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "3010:3000"
    restart: unless-stopped
